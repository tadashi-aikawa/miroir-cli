# AGENTS

## ドキュメント方針

### README.md

- 見出しは Setext 形式ではなく ATX 形式の `#` 記法を使う
- 見出しレベルは文書構造に従い、原則として文書タイトルのみを `#` にする
- 利用者向けの仕様や使い方を優先し、実装詳細は本文に書きすぎない
- 実装寄りの補足が必要な場合は `> [!INFO]` callout に分離する
- 一覧系の情報は、箇条書きより読みやすい場合は Markdown テーブルを優先する
